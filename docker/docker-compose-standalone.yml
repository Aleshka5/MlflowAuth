# MLflow only. Joins existing Postgres (network dbs) and MinIO (network s3).
# Set POSTGRES_HOST and MLFLOW_S3_ENDPOINT_URL to those container names.
#
#   podman compose -f docker/docker-compose-standalone.yml --env-file .env.mlflow up -d --build
services:
  mlflow:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: mlflow-server
    restart: unless-stopped
    ports:
      - "5000:5000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env.mlflow
    command: ["/bin/sh", "/app/docker/mlflow/start.sh"]
    networks:
      - common

networks:
  default: {}
  common:
    external: true
    name: common
